name: release
description: outer composite — delegates the publish to an inner action
runs:
  using: composite
  steps:
    - run: npm ci
      shell: bash
    - name: Publish
      uses: ./.github/actions/inner
