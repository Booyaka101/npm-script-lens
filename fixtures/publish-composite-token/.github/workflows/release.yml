name: release
on:
  push:
    tags: ['v*']
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Release
        uses: ./.github/actions/release
        with:
          npm-token: ${{ secrets.NPM_TOKEN }}
